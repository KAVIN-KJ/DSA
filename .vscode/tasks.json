{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "mkdir -p .bin && /usr/bin/g++-13 -fdiagnostics-color=always -g ${file} -o .bin/${fileBasenameNoExtension} && ./.bin/${fileBasenameNoExtension}",
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compile and run using g++-13 into .bin/"
        }
    ]
}
