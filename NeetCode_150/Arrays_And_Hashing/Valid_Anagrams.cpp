#include<bits/stdc++.h>
using namespace std;

int main(){
    string s,t;
    cin>>s;
    cin>>t;
    if(s.size()!=t.size()){
        cout<<"Invalid anagrams";
        return 0;
    }

    vector<int> mpp(26,0);
    for(int i=0;i<s.size();i++){
        
    }

}